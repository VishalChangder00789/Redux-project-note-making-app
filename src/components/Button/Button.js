import React from "react";
import "./Button.css";

const Button = () => {
  return (
    <div className="ButtonContainer">
      <button className="realButton">Add a new Note</button>
    </div>
  );
};

export default Button;
